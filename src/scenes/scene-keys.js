/**
 * @typedef {keyof typeof SCENE_KEYS} SceneKey
 */

/** @enum {SceneKey} */
export const SCENE_KEYS = Object.freeze({
  PRELOAD_SCENE: 'PRELOAD_SCENE',
  BATTLE_SCENE: 'BATTLE_SCENE',
  WORLD_SCENE: 'WORLD_SCENE',
  EVOLVE_SCENE: 'EVOLVE_SCENE'
})
