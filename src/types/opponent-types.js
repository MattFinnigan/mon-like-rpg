/**
 * @typedef {keyof typeof OPPONENT_TYPES} OpponentTypes
 */

/** @enum {OpponentTypes} */
export const OPPONENT_TYPES = Object.freeze({
  WILD_ENCOUNTER: 'WILD_ENCOUNTER',
  TRAINER: 'TRAINER',
  GYM_LEADER: 'GYM_LEADER'
})