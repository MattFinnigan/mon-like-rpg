/**
 * @typedef {keyof typeof EVENT_KEYS} EventKey
 */

/** @enum {EventKey} */
export const EVENT_KEYS = Object.freeze({
  BATTLE_RUN_ATTEMPT: 'BATTLE_RUN_ATTEMPT',
  TRAINER_BATTLE_START: 'TRAINER_BATTLE_START'
})
